import { createStyleSheet } from '@/helpers';

export const styles = createStyleSheet({
  wrapper: {
    padding: '10px',
  },
  nameInput: {
    '.MuiInputBase-input': {
      fontSize: '14px',
      padding: '5.5px 10px',
    },
  },
});
