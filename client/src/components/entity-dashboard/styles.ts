import { createStyleSheet } from '@/helpers';

export const styles = createStyleSheet({
  progress: {
    position: 'absolute',
    top: 0,
    width: '100%',
    left: 0,
  },
  dashboardWrapper: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
  },
});
