import { createStyleSheet } from '@/helpers';

export const styles = createStyleSheet({
  authPageWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});
